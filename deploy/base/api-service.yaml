apiVersion: v1
kind: Service
metadata:
  name: hello-app-api
  labels:
    app.kubernetes.io/name: hello-app-api
    app.kubernetes.io/part-of: hello-app
spec:
  selector:
    app.kubernetes.io/name: hello-app-api
  ports:
    - name: http
      port: 80
      targetPort: http
