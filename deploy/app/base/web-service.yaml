apiVersion: v1
kind: Service
metadata:
  name: hello-app-fork-web
  labels:
    app.kubernetes.io/name: hello-app-fork-web
    app.kubernetes.io/part-of: hello-app-fork
spec:
  selector:
    app.kubernetes.io/name: hello-app-fork-web
  ports:
    - name: http
      port: 80
      targetPort: http
